from setuptools import setup, find_packages

setup(
    name="src",
    version = "0.0.1",
    description="Its a Wine-Ops Package",
    author = "CycloneLights",
    packages = find_packages(), ##We can also specify list for hardcoding
    license="MIT"

)